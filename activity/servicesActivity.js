import React from 'react'
import PropTypes from 'prop-types'
import Text from 'react-native'

const ServicesActivity = React.createClass({
  render () {
    return (
      <Text style={{color: 'white'}}>Basic modal</Text>
    )
  }
})

export default ServicesActivity
